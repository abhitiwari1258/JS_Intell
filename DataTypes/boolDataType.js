// new = "Create a new instance of a class/constructor."
// An instance is simply an object created from a class

const x = true
console.log(x);

const y = new Boolean(true)
console.log(y)

console.log(x == y)
console.log(x === y)  // one is bool another is boolean obj
